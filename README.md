# Portfolio

Portfolio website project.

## Development

```bash
npm install
npm run dev
```

The local app runs on the Vite URL printed in the terminal, usually
`http://localhost:5173`.

## Build

```bash
npm run build
```

## Design

The visual direction is documented in `DESIGN.md`, with the current reference image in `screen.png`.
